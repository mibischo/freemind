# Required metadata
sonar.projectKey=freemind
sonar.projectName=freemind :: Open source mind map
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=freemind

# Language
sonar.language=java

# Encoding of the source files
sonar.sourceEncoding=UTF-8